# SPDX-License-Identifier: Apache-2.0
#
# SoftAP HTTP provisioning only (e.g. ESP32-S2 without Bluetooth).
# Merge: west build ... -- -DEXTRA_CONF_FILE=overlay-softap-only.conf

CONFIG_ESP_PROV_USE_BLE=n
CONFIG_BT=n
