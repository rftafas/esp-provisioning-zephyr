# SPDX-License-Identifier: Apache-2.0
#
# Espressif SoCs without Bluetooth (e.g. ESP32-S2): SoftAP HTTP provisioning only.
# Merge: west build ... -- -DEXTRA_CONF_FILE=conf-softap-only.conf

CONFIG_ESP_PROV_USE_BLE=n
CONFIG_BT=n
