# SPDX-License-Identifier: Apache-2.0
#
# SoCs without Wi-Fi / SoftAP path (e.g. ESP32-H2): BLE GATT provisioning only.
# Merge: west build ... -- -DEXTRA_CONF_FILE=conf-ble-only.conf

CONFIG_ESP_PROV_USE_SOFTAP=n
