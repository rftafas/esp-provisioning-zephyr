# SPDX-License-Identifier: Apache-2.0
#
# BLE GATT provisioning only (e.g. ESP32-H2, or to save flash/RAM on Wi-Fi SoCs).
# Merge: west build ... -- -DEXTRA_CONF_FILE=overlay-ble-only.conf

CONFIG_ESP_PROV_USE_SOFTAP=n
